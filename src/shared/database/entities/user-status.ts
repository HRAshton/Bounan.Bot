export enum UserStatus {
    // ACTIVE: User is active
    ACTIVE = 0,

    // READ_ONLY: Can request existing episodes, but cannot initiate new downloads
    // READ_ONLY = 1,

    // SUSPENDED: User is suspended
    SUSPENDED = 2,
}